# Create forms.py in the el app directory
touch el/forms.py

# Create validators.py in the el app directory
touch el/validators.py

# Create urls.py in the el app directory for app-specific URL configurations
touch el/urls.py

# Create routing.py in the main project directory for WebSocket routing
touch elearning/routing.py

# Create templates directory and a subdirectory for the el app
mkdir -p el/templates/el

# Create consumers.py in the el app directory for WebSocket consumers
touch el/consumers.py
